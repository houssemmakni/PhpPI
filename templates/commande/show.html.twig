{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ commande.nombre }}</td>
            </tr>
            <tr>
                <th>Addresse_commande</th>
                <td>{{ commande.addresseCommande }}</td>
            </tr>
            <tr>
                <th>Nom_prenom_commande</th>
                <td>{{ commande.nomPrenomCommande }}</td>
            </tr>
            <tr>
                <th>Tel_commande</th>
                <td>{{ commande.telCommande }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_index') }}">back to list</a>

    <a href="{{ path('app_commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
