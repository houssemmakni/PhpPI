{% extends 'base-back.html.twig' %}

{% block title %}Edit Reclamation{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="display-4 text-primary mb-4">Edit Reclamation</h1>

        <div class="card p-4 mb-4">
            {{ include('reclamation/_form1.html.twig', {'button_label': 'Update'}) }}
        </div>
        
        <a href="{{ path('app_reclamation_table') }}" class="btn btn-primary">
            <i class="bi bi-arrow-left"></i> Back to List
        </a>
    </div>

{% endblock %}
